## 1. 项目骨架与目录结构

- [x] 1.1 按设计创建 `src/app/`、`src/features/auth/`、`src/features/workbench/`、`src/features/knowledge-tree/`、`src/features/inbox/`、`src/features/detail-drawer/`、`src/features/sync/`、`src/shared/`、`src/db/`、`src/supabase/` 目录。
- [x] 1.2 建立 `main.tsx`、`App.tsx`、`AppShell.tsx`、`ThemeProvider`、`AuthProvider` 和 `QueryProvider` 等应用级入口文件。
- [x] 1.3 补齐 `shared/components`、`shared/hooks`、`shared/utils`、`shared/types`、`shared/styles` 的基础导出和通用工具。

## 2. 路由壳与认证流程

- [x] 2.1 实现 `/`、`/login`、`/auth/callback`、`/app` 四个路由及其守卫逻辑。
- [x] 2.2 实现产品入口页与独立登录页的基础布局，保留左侧品牌展示区和右侧表单区。
- [x] 2.3 实现登录页的邮箱密码、Magic Link、OAuth 和注册模式切换。
- [x] 2.4 实现 `/auth/callback` 的会话接收、落盘、清理临时参数和跳转 `/app`。
- [x] 2.5 实现未登录访问 `/app` 时的引导流程，以及已登录访问 `/login` 时自动回到工作台。

## 3. 本地数据层与同步引擎

- [x] 3.1 建立 Dexie schema，覆盖 `knowledge_tree_node`、`excerpts`、`tags`、`excerpt_tags`、本地视图偏好和同步游标。
- [x] 3.2 实现本地 repository 和 mapper，保证工作台优先从 Dexie 初始化。
- [x] 3.3 封装 Supabase client、auth 和 sync 请求入口。
- [x] 3.4 实现增量推送、增量拉取、Realtime 触发拉取和断网重试。
- [x] 3.5 实现 `device_id`、`update_time`、`is_deleted` 的冲突处理与软删除策略。

## 4. 工作台状态与三栏布局

- [x] 4.1 建立 Zustand 工作台状态，管理当前选中节点、抽屉开关、收件箱筛选、画布视口、搜索弹窗、主题模式和展开状态。
- [x] 4.2 实现三栏式主工作台布局，包含左侧收件箱、中间知识树画布和右侧详情抽屉。
- [x] 4.3 实现顶部统一导航栏，包含品牌、全局搜索、主题切换和账户入口。
- [x] 4.4 持久化并恢复展开状态、视口位置、侧栏宽度、最近搜索和页面上下文。

## 5. 知识树画布

- [x] 5.1 实现平铺节点到画布节点的适配层，支持虚拟根节点和局部预览数据。
- [x] 5.2 使用 React Flow + Dagre 实现从左到右的知识树布局。
- [x] 5.3 实现节点标题、笔记标记、摘录计数、折叠/展开与快捷操作按钮。
- [x] 5.4 实现 `Tab`、`Enter`、方向键、`Space` 的键盘优先建树与导航交互。
- [x] 5.5 实现 MiniMap、放大、缩小和 Fit View 视图控制。
- [x] 5.6 实现 `sort_order` 稀疏排序和第一层默认展开、后续本地展开记忆。

## 6. 收件箱侧栏

- [x] 6.1 实现未挂载摘录与全部摘录两种筛选视图。
- [x] 6.2 实现卡片内容展示，覆盖正文、来源、我的思考和标签。
- [x] 6.3 实现收件箱卡片拖拽到画布节点时的高亮反馈与挂载投放。
- [x] 6.4 实现挂载后从收件箱移除、目标节点计数加一和无效投放回退。

## 7. 详情抽屉

- [x] 7.1 实现右侧详情抽屉的节点上下文切换与全屏专注写作模式。
- [x] 7.2 使用 Tiptap 实现 Markdown 大纲编辑器，并支持块级输入和 Slash 菜单。
- [x] 7.3 实现当前节点下摘录流展示、我的思考快速编辑、标签展示和移回未分类。
- [x] 7.4 实现标签多选选择器和挂载位置选择器，并将保存结果写回本地数据层。

## 8. 搜索、主题与页面恢复

- [x] 8.1 实现全局搜索弹窗，展示最近搜索、节点结果和内容结果。
- [x] 8.2 实现浅色/深色主题切换及其本地持久化。
- [x] 8.3 实现刷新后恢复当前工作上下文，包括选中节点、抽屉状态和画布位置。

## 9. 验证与收尾

- [x] 9.1 为关键路由、工作台骨架、画布节点、收件箱和详情抽屉补充 Preview。
- [x] 9.2 为同步、挂载、编辑和路由跳转补充针对性测试。
- [x] 9.3 在 `glean-read-web` 中执行最小必要构建与验证，确认目录结构、路由和基础页面可运行。
