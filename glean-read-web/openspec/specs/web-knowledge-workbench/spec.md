# 规范：web-knowledge-workbench

## Purpose
定义 GleanRead Web 端主工作台的路由壳、全局导航和三栏页面骨架，确保用户在 Web 端能够从登录入口进入受保护工作台，并在同一页面框架内完成摘录浏览、知识树构建和右侧沉淀。

## Requirements
### Requirement: Web 端必须提供独立的路由壳与受保护工作区
系统 MUST在 Web 端提供 `/`、`/login`、`/auth/callback` 和 `/app` 四个路由，并保证它们承担各自固定职责：`/` 作为产品入口，`/login` 作为独立登录页，`/auth/callback` 作为认证回调收敛页，`/app` 作为受保护的主工作台。未完成认证的用户访问 `/app` 时，系统 MUST引导其进入登录流程而不是直接暴露工作台内容。

#### Scenario: 打开产品入口页
- **WHEN** 用户直接访问 `/`
- **THEN** 系统 MUST展示 GleanRead 的产品入口和开始使用入口
- **THEN** 页面不得直接进入受保护的工作台内容

#### Scenario: 访问受保护工作区
- **WHEN** 未认证用户直接访问 `/app`
- **THEN** 系统 MUST将其引导到登录入口或等价的认证流程
- **THEN** 系统不得在未认证状态下渲染工作台主体内容

#### Scenario: 处理认证回调
- **WHEN** 用户从邮箱验证、OAuth 或其他认证跳转进入 `/auth/callback`
- **THEN** 系统 MUST完成凭证接收、会话落盘与状态恢复
- **THEN** 成功处理后系统 MUST重定向到 `/app`

### Requirement: 主工作台必须以三栏骨架承载核心知识流
系统 MUST在 `/app` 提供固定的三栏工作台骨架：左侧为收件箱侧栏，中间为横向知识树画布，右侧为详情沉淀抽屉。桌面端主布局必须保持左中右三栏同时可见，默认宽度目标分别约为 280px、剩余自适应空间和 400px；其中左侧侧栏与右侧抽屉可以独立折叠或展开，但中间画布始终是页面主体。

#### Scenario: 首次进入工作台
- **WHEN** 用户登录后首次进入 `/app`
- **THEN** 系统 MUST显示左侧收件箱、中间画布和右侧抽屉的工作台骨架
- **THEN** 页面不得退化为单栏或跳转式多页面界面

#### Scenario: 切换侧栏可见性
- **WHEN** 用户折叠左侧收件箱或展开右侧抽屉
- **THEN** 系统 MUST仅调整对应栏位的可见性与宽度
- **THEN** 中间画布的主体定位关系必须保持稳定

### Requirement: 顶部导航必须提供全局搜索、主题切换与账户入口
系统 MUST在工作台顶部提供统一导航栏，且导航栏必须包含品牌标识、全局搜索入口、主题切换入口以及账户或设置入口。全局搜索必须能从键盘快捷键 `Ctrl + K` 或 `Cmd + K` 唤出，并在关闭后返回原工作台上下文。

#### Scenario: 打开全局搜索
- **WHEN** 用户点击顶部搜索入口或按下 `Ctrl + K` / `Cmd + K`
- **THEN** 系统 MUST打开全局搜索弹窗
- **THEN** 用户关闭弹窗后必须回到原来的工作台视图

#### Scenario: 切换主题
- **WHEN** 用户点击顶部主题切换入口
- **THEN** 系统 MUST在浅色和深色工作模式之间切换
- **THEN** 切换结果必须立即反映到当前工作台的视觉样式

#### Scenario: 进入账户相关操作
- **WHEN** 用户点击顶部账户或设置入口
- **THEN** 系统 MUST提供账户相关操作入口
- **THEN** 工作台布局不得因此被整体替换为其他页面

### Requirement: 工作台页面状态必须可恢复并保持上下文
系统 MUST在 Web 工作台内维护当前选中节点、侧栏展开状态、抽屉开关和视图位置等页面级状态，并在刷新或重新进入时尽可能恢复用户的工作上下文。左侧收件箱和右侧抽屉的内容切换不得破坏中间画布的当前浏览位置。

#### Scenario: 刷新后恢复工作台上下文
- **WHEN** 用户在工作台中刷新页面
- **THEN** 系统 MUST尽可能恢复上一次的页面级状态
- **THEN** 用户不得被重置回完全无上下文的初始视图

#### Scenario: 切换选中节点
- **WHEN** 用户在中间画布中切换当前选中节点
- **THEN** 系统 MUST更新右侧抽屉所对应的内容上下文
- **THEN** 左侧收件箱列表的浏览位置不得被无关重置
