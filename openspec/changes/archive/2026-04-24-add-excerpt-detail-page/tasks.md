## 1. 数据与写入链路

- [x] 1.1 为 `ExcerptTagDao` 增加编辑已有摘录标签关系所需的查询与更新能力，支持读取某条摘录当前的标签关系并写回新增/移除后的关系状态。
- [x] 1.2 在 `SnapshotRepository` 中新增摘录详情页保存入口，统一处理正文、想法、来源标题、来源链接、挂载位置和标签关系的本地更新事务。
- [x] 1.3 在 `MainAppViewModel` 和依赖注入容器中暴露摘录详情页保存所需回调，保证 UI 不直接访问 Repository。

## 2. 导航与入口统一

- [x] 2.1 在 `MainRoutes` 与 `MainApp` 中新增摘录详情页路由，并接入 `excerpt/{excerptId}` 导航参数。
- [x] 2.2 将摘录流卡片点击、节点详情挂载摘录、Backlinks、局部图谱摘录节点和展示态摘录链接的单摘录入口全部切换到摘录详情页路由。
- [x] 2.3 移除 `previewExcerptId` 与 `ExcerptPreviewDialog` 的旧路径，避免工作台保留双重“预览/详情”行为。

## 3. 摘录详情页基础结构

- [x] 3.1 新增 `feature/excerpts/detail` 页面结构，实现 `ExcerptDetailRoute` 与 `ExcerptDetailScreen`，并按现有模式管理编辑态草稿。
- [x] 3.2 使用 `Scaffold`、可收起顶栏和 `LazyColumn` 实现阅读态布局，按元信息、摘录内容、我的想法、来源的顺序渲染页面。
- [x] 3.3 实现阅读态与编辑态切换，以及“仅在有变更时启用保存”的 dirty 判断逻辑。
- [x] 3.4 补充摘录详情页所需字符串资源、Preview 假数据和必要的可访问性文案。

## 4. 标签与挂载编辑

- [x] 4.1 在详情页编辑态为标签和挂载位置增加显式编辑入口，并让元信息区在阅读态和编辑态保持一致的层级结构。
- [x] 4.2 复用 `KnowledgeTreeNodePickerBottomSheet` 接入挂载位置编辑，支持切换到其他节点或改回 Inbox。
- [x] 4.3 实现详情页私有的标签多选选择器，支持展示当前已选标签、搜索本地标签和切换勾选状态。
- [x] 4.4 让详情页保存流程把标签和挂载位置的变更一起落库，并在返回阅读态后立即刷新元信息显示。

## 5. 清理与验证

- [x] 5.1 清理导航和命名中的旧 `onPreviewExcerpt` 语义，统一为打开摘录详情页的回调表达。
- [ ] 5.2 检查保存后的本地回显，确认摘录流、节点详情、Backlinks、局部图谱和展示态摘录链接都能看到最新的正文、标签和挂载状态。
- [x] 5.3 在 `E:\program\jdk21` 环境下运行最小必要验证，至少覆盖 `:app:compileDebugKotlin`，并在条件允许时补充受影响逻辑的单元测试或针对性验证。
