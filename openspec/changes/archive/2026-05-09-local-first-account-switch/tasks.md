## 1. 数据库底层支持

- [x] 1.1 重构底层 Room 数据库获取逻辑，支持按名称获取独立的 DB 连接实例。
- [x] 1.2 提供访客专属 `guest.db` 的初始化与获取入口。
- [x] 1.3 提供正式用户 `local_data_{user_id}.db` 的动态切换和懒加载实例化能力。

## 2. 账号状态与数据库切换

- [x] 2.1 监听 Supabase 登录状态，在未登录或登出状态下，将应用当前的有效 DB 连接绑定至 `guest.db`。
- [x] 2.2 登录成功时，根据获取的 `user_id` 初始化并绑定当前连接至 `local_data_{user_id}.db`。
- [x] 2.3 验证同设备多账号切换场景下的”0延迟”直接读取已存在的本地 `.db` 文件的效果。

## 3. 访客数据迁移合并

- [x] 3.1 编写从 `guest.db` 读取所有记录并批量写入到目标 `local_data_{user_id}.db` 的数据迁移服务。
- [x] 3.2 拦截从访客态完成登录的回调，检查 `guest.db` 中是否存在数据。
- [x] 3.3 若存在数据，弹窗询问用户是否合并，同意则调用迁移服务并在完成后清空/关闭 `guest.db`。

## 4. 登出流程的安全退出与队列拦截

- [x] 4.1 修改用户登出操作，前置检查当前用户的同步队列中是否存在未同步的修改记录。
- [x] 4.2 如果队列不为空，弹出”未同步数据警告”弹窗拦截用户退出。
- [x] 4.3 登出成功后，主动调用 `WorkspaceDatabaseManager.closeCurrentDatabase()` 切回 `guest.db`。

## 5. 存储空间管理与自动清理

- [x] 5.1 在 App 设置页的 `SyncSection` 同级增加”清除本地缓存”操作按钮。
- [x] 5.2 实现手动清理服务，支持在用户点击按钮后，关闭所有 DB 连接，删除除了当前活跃数据库以外的所有 `.db` 文件。
- [x] 5.3 （可选/后续）实现超期 30 天自动清理后台任务。
