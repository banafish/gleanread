## 1. 项目基础与依赖

- [x] 1.1 为 `glean-read-android/app/build.gradle.kts` 添加 Room、Navigation Compose、Lifecycle/ViewModel 与 Room 编译器依赖，并确保 Compose 配置可支撑主工作台实现
- [x] 1.2 规划并创建 Android 主工作台、本地数据层、图谱视图和联想编辑器所需的包结构与基础文件入口

## 2. Room 本地数据层

- [x] 2.1 按文档定义实现 `knowledge_tree_node`、`tags`、`excerpts`、`excerpt_tags` 的 Room Entity、DAO、Database 与类型映射
- [x] 2.2 实现 Repository 与本地查询模型，支持 Inbox、知识树层级、标签分组、节点详情、Backlinks 与待同步状态维护
- [x] 2.3 实现示例数据 Seeder 与空库检测逻辑，支持空状态引导后显式导入 `doc/ui.tsx` 对齐的示例数据

## 3. 主工作台与导航框架

- [x] 3.1 用 Navigation Compose 重写 `MainActivity`，搭建摘录流、知识树、标签、AI 提炼页、节点详情页和图谱视图的导航骨架
- [x] 3.2 按 `doc/ui.tsx` 实现底部导航、全局 FAB、多选底部操作栏与页面级显隐规则
- [x] 3.3 实现空状态引导页，并接通“加载示例数据”和“开始记录”两个入口

## 4. 摘录流与快速记录面板

- [x] 4.1 按 `doc/ui.tsx` 实现摘录流页面的搜索区、筛选入口、摘录卡片、AI 推荐卡和长按多选交互
- [x] 4.2 实现全局 FAB Bottom Sheet，支持自动聚焦、遮罩关闭、正文输入、可选 URL、标签推荐与默认 Inbox 保存
- [x] 4.3 将快速记录保存动作接入本地 Repository，只写 Room 并正确维护标签热度、摘录关系和待同步标记

## 5. 本地 AI 提炼与知识树挂载

- [x] 5.1 实现本地 OutlineGenerator 与 AI 提炼页数据流，支持多选摘录生成可编辑大纲草稿
- [x] 5.2 按 `doc/ui.tsx` 实现节点选择器，支持选择现有节点或在本地创建新子节点作为挂载目标
- [x] 5.3 实现 AI 整理结果保存逻辑，把大纲和摘录挂载关系写回本地库，并刷新摘录流、知识树与节点详情

## 6. 知识树、节点详情与局部图谱

- [x] 6.1 按 `doc/ui.tsx` 实现知识树页面的层级展开/收起、节点点击跳转和根节点创建入口
- [x] 6.2 实现节点详情页的大纲展示、摘录列表、Backlinks 区域与 `[[` 链接点击跳转
- [x] 6.3 实现局部关系图视图，支持基于本地数据构图、拖拽、缩放、点击聚焦以及节点/摘录跳转或预览

## 7. `[[` 联想编辑与文本解析

- [x] 7.1 在快速记录面板和节点/AI 编辑输入区实现 `[[` 触发检测、本地候选搜索与联想面板展示
- [x] 7.2 实现结构化链接文本的插入、持久化与展示态解析，支持知识节点和摘录两类目标
- [x] 7.3 将链接解析结果接入节点详情、摘录展示与局部图谱构建所需的本地引用查询

## 8. 收尾验证

- [x] 8.1 补齐页面状态、返回栈、多选取消、空状态切换和示例数据导入后的界面联动验证
- [x] 8.2 运行 Android 构建/静态检查命令，确认新增本地工作台代码可以成功编译
